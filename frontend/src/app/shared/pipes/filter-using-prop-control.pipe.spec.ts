import { FilterUsingPropControlPipe } from "./filter-using-prop-control.pipe";

describe("FilterUsingPropControlPipe", () => {
    it("create an instance", () => {
        const pipe = new FilterUsingPropControlPipe();
        expect(pipe).toBeTruthy();
    });
});
